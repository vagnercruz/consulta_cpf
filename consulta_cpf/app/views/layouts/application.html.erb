<!DOCTYPE html>
<html lang="pt-br" data-theme="light">
<head>
  <meta charset="UTF-8">
  <title>Consulta de CPF</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Ícone -->
  <link rel="icon" href="https://cdn-icons-png.flaticon.com/512/711/711284.png">

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <style>
    body {
      background: var(--bg);
      color: var(--text);
      transition: 0.3s ease;
    }

    :root[data-theme='light'] {
      --bg: #f4f7ff;
      --card: white;
      --text: #222;
      --primary: #0d6efd;
    }

    :root[data-theme='dark'] {
      --bg: #0d1117;
      --card: #161b22;
      --text: #e6edf3;
      --primary: #3ea6ff;
    }

    .card-custom {
      background: var(--card);
      border-radius: 20px;
      padding: 30px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.1);
      transition: 0.3s ease;
    }

    input {
      background: var(--card) !important;
      color: var(--text) !important;
      border-color: var(--primary) !important;
    }

    input:focus {
      box-shadow: 0 0 8px rgba(13,110,253,0.4);
    }

    .btn-outline-primary {
      border: 2px solid var(--primary);
      color: var(--primary);
    }

    .btn-outline-primary:hover {
      background: var(--primary);
      color: white !important;
      box-shadow: 0 0 10px rgba(13,110,253,0.5);
    }

    .theme-toggle {
      cursor: pointer;
      font-size: 22px;
      margin-left: 10px;
    }
  </style>

  <script>
    function toggleTheme() {
      const html = document.documentElement;
      const theme = html.getAttribute("data-theme");

      html.setAttribute("data-theme", theme === "light" ? "dark" : "light");
    }
  </script>

</head>

<body>
  <nav class="p-3 d-flex justify-content-between align-items-center" style="background: var(--card);">
    <h3 class="m-0">🔍 Consulta CPF</h3>

    <div onclick="toggleTheme()" class="theme-toggle">
      🌙 / ☀️
    </div>
  </nav>

  <div class="container mt-5">
    <%= yield %>
  </div>

</body>
</html>
