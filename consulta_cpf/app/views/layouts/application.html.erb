<!DOCTYPE html>
<html lang="pt-br" data-theme="light">
<head>
  <meta charset="UTF-8">
  <title>Consulta de CPF</title>

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

  <script>
    function toggleTheme() {
      const html = document.documentElement;
      const theme = html.getAttribute("data-theme");
      html.setAttribute("data-theme", theme === "light" ? "dark" : "light");
    }
  </script>

</head>

<body>
  <nav class="p-3 d-flex justify-content-between align-items-center shadow-sm" style="background: var(--card);">
    <h3 class="m-0">🔍 Consulta CPF</h3>
    <div onclick="toggleTheme()" class="theme-toggle" style="cursor:pointer;font-size:22px;">
      🌙 / ☀️
    </div>
  </nav>

  <div class="container mt-5 mb-5">
    <%= yield %>
  </div>

</body>
</html>
